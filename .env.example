# Example Environment Variables for ResQnet
# Copy this file to .env or set these as system environment variables

# Database Configuration
DB_HOST=your-database-host.com
DB_PORT=3306
DB_NAME=resqnet
DB_SSL_MODE=DISABLED
DB_USER=your_database_user
DB_PASS=your_database_password

# Note: Never commit the actual .env file or application.properties with real credentials to version control
